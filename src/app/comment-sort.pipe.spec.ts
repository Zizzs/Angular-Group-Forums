import { CommentSortPipe } from './comment-sort.pipe';

describe('CommentSortPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentSortPipe();
    expect(pipe).toBeTruthy();
  });
});
